import {View, Text} from 'react-native';
import React from 'react';
import {Intro_RealTime as Intro_RealTimeComp} from '../features';

const Intro_RealTimeScreen = props => {
  return <Intro_RealTimeComp props={props} />;
};

export default Intro_RealTimeScreen;
