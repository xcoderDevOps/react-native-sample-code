import {View, Text} from 'react-native';
import React from 'react';
import {EventDetails} from '../features';
const EventDetailsScreen = props => {
  return <EventDetails props={props} />;
};

export default EventDetailsScreen;
