import {View, Text} from 'react-native';
import React from 'react';
import {StatisticsFilter} from '../features';

const StatisticsFilterScreen = props => {
  return <StatisticsFilter props={props} />;
};

export default StatisticsFilterScreen;
