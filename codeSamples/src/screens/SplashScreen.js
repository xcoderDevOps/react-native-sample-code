import {View, Text} from 'react-native';
import React from 'react';
import {Splash as SplashComp} from '../features';

const SplashScreen = props => {
  return <SplashComp props={props} />;
};

export default SplashScreen;
