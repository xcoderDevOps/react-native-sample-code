import { View, Text } from 'react-native'
import React from 'react'
import {Signup as Signup} from '../features'
const SignupScreen = (props) => {
  return (
   <Signup props={props}/>
  )
}

export default SignupScreen