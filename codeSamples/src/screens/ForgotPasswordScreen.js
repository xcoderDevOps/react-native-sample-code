import {View, Text} from 'react-native';
import React from 'react';
import { ForgetPassword } from '../features';

const ForgotPasswordScreen = (props) => {
  return (
    <ForgetPassword props={props}/>
  );
};

export default ForgotPasswordScreen;
